package com.ndumas.appdt.presentation.automation.create.trigger

enum class TriggerUiType {
    NUMERIC, // Mostra operatori (>, <, =) e input numerico
    STATE, // Mostra dropdown valori (on, off, open, closed)
}
